{
  "name": "kc-floating-chatbot",
  "version": "1.0.0",
  "private": true,
  "description": "Floating knowledge-base chatbot for Bubble apps",
  "scripts": {
    "dev": "concurrently -n server,widget -c blue,green \"npm run dev -w server\" \"npm run dev -w widget\"",
    "build": "npm run build -w widget && npm run build -w server",
    "start": "npm run start -w server",
    "dev:server": "npm run dev -w server",
    "dev:widget": "npm run dev -w widget"
  },
  "workspaces": [
    "server",
    "widget"
  ],
  "devDependencies": {
    "concurrently": "^9.1.2"
  }
}
